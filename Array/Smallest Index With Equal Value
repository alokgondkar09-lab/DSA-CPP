class Solution {
public:
    int smallestEqual(vector<int>& arr) {
        int n = arr.size();
        // sort(arr.begin(),arr.end());
        for(int i=0;i<n;i++){
            if( i%10 == arr[i] ){
                return i;
            }
        }
        return -1;
    }
};
