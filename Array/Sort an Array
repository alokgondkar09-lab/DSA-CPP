class Solution {
public:
    vector<int> sortArray(vector<int>& arr) {
        int n = arr.size();
        int i = 0;
        int j = n - 1;
        
        while(i<j){
            if(arr[i]>arr[j]){
                swap(arr[i],arr[j]);
                i++;
                j--;
            }
            if(arr[i]<arr[j]){
                i++;
                j--;
            }
        }
        return arr;
    }
};    WRONG METHOD
