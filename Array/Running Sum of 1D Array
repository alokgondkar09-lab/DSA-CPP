class Solution {
public:
    vector<int> runningSum(vector<int>& arr) {
        int n = arr.size();
        int a = 0;
        vector<int> ans;
        for(int i=0;i<n;i++){
            a += arr[i];
            ans.push_back(a);
        }
        return ans;
    }
};
